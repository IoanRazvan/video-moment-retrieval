{
  "configurations": [
    {
      "name": "Clip feature extractor",
      "type": "debugpy",
      "request": "launch",
      "module": "video_moment_retrieval.moment_detr.model",
      // "args": "${command:pickArgs}"
    }
  ]
}
